\section{Introduction}
\label{sec:intro}

Estimating metric depth and reconstructing 3D surface geometry from a single monocular image is a fundamental challenge in computer vision and autonomous robotics~\cite{eigen2014depth,godard2019digging,ranftl2020towards}. Precise depth maps serve as the sensory backbone for obstacle avoidance, visual-inertial odometry, 3D mapping, and augmented reality. While active sensors (LiDAR, structured light) provide accurate metric range, they introduce payload, power, and cost constraints that restrict their deployment on micro-aerial vehicles (MAVs), lightweight robotics, and wearable devices.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_benchmark.png}
    \caption{\textbf{Qualitative In-Domain Case Study on Raw Ground Truth Depth Array (Frame \texttt{000025}, Office Lobby) (Stage~1 Baseline Model, Canonical $\mathbf{K}$)}. From left to right: (1) Input RGB frame from the in-domain \textit{office} environment; (2) Ground truth metric depth; (3) Dioptra Stage~1 baseline prediction ($\text{AbsRel} = 0.2291$); (4) Absolute log-ratio error heatmap $|\log(\hat{D}) - \log(D)|$, showing consistent scale estimation across the continuous floor, ceiling, and deep receding corridor.}
    \label{fig:teaser}
\end{figure}

In recent years, the paradigm of dense depth estimation has increasingly shifted toward large vision foundation models~\cite{ranftl2021vision,yang2024depth,yang2024depth2,yin2023metric3d}. Frameworks such as Depth Anything~\cite{yang2024depth} and Metric3D~\cite{yin2023metric3d} achieve strong visual representations by pairing massive transformer backbones ($>300$\,M parameters) with internet-scale synthetic and pseudo-labeled datasets. Concurrently, lightweight architectures such as Lite-Mono~\cite{zhang2023litemono}, FastDepth~\cite{wofk2019fastdepth}, and Depth Anything v2-Small~\cite{yang2024depth2} have demonstrated efficient inference on constrained hardware. However, several fundamental geometric challenges remain:
\begin{enumerate}
    \item \textbf{Decoupled Camera Geometry}: Standard vision transformers treat input images as uniform 2D grid tokens with 2D coordinate positional embeddings, ignoring the physical optical rays defined by the camera intrinsic calibration matrix $\mathbf{K}$~\cite{piccinelli2024unidepth}.
    \item \textbf{Scale Ambiguity}: Monocular models trained without explicit scale regularization frequently collapse to predicting affine-invariant relative depth, requiring test-time scale alignment that precludes direct obstacle ranging in metres.
    \item \textbf{Edge Memory Envelopes}: Embedded robotics platforms operate with tight unified memory budgets, where architectures requiring large intermediate activation buffers can induce out-of-memory faults.
\end{enumerate}

To address these limitations, we introduce \textbf{Dioptra} (Figure~\ref{fig:teaser}), a compact, geometry-grounded architecture ($8.1$\,M parameters) designed for efficient metric depth estimation. Our primary contributions are:
\begin{itemize}
    \item \textbf{Trivision Ray Positional Encoding}: An optical ray triplet unprojection scheme that incorporates camera intrinsic calibration directly into transformer token space with horizontal reflection tracking.
    \item \textbf{Angular Residual Attention (ARA)}: A continuous geometric attention bias computing 3D angular residuals $\sin^2(\theta_{q, k}) = 1 - (\hat{\mathbf{r}}_q \cdot \hat{\mathbf{r}}_k)^2$ to sharpen planar surfaces and structural boundaries.
    \item \textbf{Decoupled Metric Supervision}: A dual objective combining unnormalized raw metric losses with an explicit log-median scale consistency loss ($\mathcal{L}_{\text{scale}}$) and multi-task uncertainty weighting.
    \item \textbf{Camera-Intrinsic Invariance via Dynamic Pinhole Training}: We identify that fixed-camera training induces unprojection degeneracies, and show that dynamic pinhole crop augmentation empirically validates camera-intrinsic equivariance, reducing telephoto error by up to \textbf{$-55.9\%$} ($>2.2\times$ accuracy improvement) across multi-FOV sweeps ($50^\circ \text{--} 100^\circ$).
    \item \textbf{Empirical Cross-Environment Evaluation}: On $8,186$ held-out TartanAir test frames across unseen 3D maps, Dioptra Stage~1 achieves $0.3073$ Aligned AbsRel ($0.9293$ Metric AbsRel), while Stage~2 dynamic pinhole fine-tuning delivers our headline \textbf{$0.2901$ Aligned AbsRel} and \textbf{$0.6043$ Metric AbsRel} ($35.0\%$ error reduction). Zero-shot evaluation on 40 uncompressed ground-truth test arrays validates resilient transfer across daylight, night, and outdoor conditions ($0.7420$ Stage~2 Metric AbsRel; Table~\ref{tab:ablation_study}).
    \item \textbf{Edge Efficiency and Real-Time Profiling}: With only $8.1$\,M parameters ($32.4$\,MB FP32 weights) and $<180$\,MB peak RAM, Dioptra executes steady-state forward inference in $52.8$\,ms ($\approx 18.9$\,FPS) on Apple Silicon M3 GPU and $28.2$\,ms on NVIDIA T4, validating real-time metric spatial perception on low-power embedded platforms.
\end{itemize}

